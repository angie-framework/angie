'use strict';

import $injector from '../../src/services/$Injector';
import $LogProviders from '../../src/util/$LogProvider';

// TODO assign simple mock to constant

describe('$Injector', () =>
    beforeEach(() =>
        // TODO mock process.exit
        // TODO mock $log.error
    );
    describe('get', () =>

    );
);

function someTestFn1(test, test2, test3) {
    return;
}

function someTestFn2(test, test2, test3) {
    return;
}
